#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int romanToInt(char *)

int main()
{
    // printf('%d\n', romanToInt("LVIII"));
    romanToInt("LVIII")
    return 0;
}

int romanToInt(char * s)
{
    for(int i = 0; i < strlen(s); i++) {
        printf("%c ", s[i]);
    }
}